import 'package:flutter/material.dart';

class AddRecordView extends StatelessWidget {
  const AddRecordView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('AddRecordView'),
      ),
    );
  }
}
